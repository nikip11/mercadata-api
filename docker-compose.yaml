services:
  app:
    build: .
    tty: true
    container_name: app_py
    volumes:
      - ./app:/app:cached
    working_dir: /app
    ports:
      - "80:8000"
    expose:
      - 80
    environment:
      - DB_URL=DB_URL_MERCADATA
