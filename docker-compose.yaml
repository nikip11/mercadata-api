services:
  app:
    build: .
    tty: true
    container_name: app_py
    volumes:
      - ./app:/app:cached
    working_dir: /app
    ports:
      - "8888:80"
    expose:
      - 8888
    environment:
      - DB_URL=DB_URL_MERCADATA
